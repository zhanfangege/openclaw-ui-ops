# 2026-03-03

## SSH落地能力自检（10:53 UTC）

- Decision: 先做能力体检与skills盘点；确认可直接执行SSH配置，若涉及安全加固优先使用现有healthcheck流程。
- Current status: 本机已具备SSH客户端（`/usr/bin/ssh`，OpenSSH_9.2p1）；`npx skills`可用；项目级已安装skill仅`find-skills`；全局skills为空；通过`skills find "ssh hardening"`发现可选外部skills（含`mikr13/secure-server-setup-skills@ssh-hardening`等）。
- Next step: 用户提供目标主机与配置目标后，直接执行首轮连通+权限检查并落地配置；若你同意，可按需补装1个SSH加固skill辅助标准化。
- Blockers: 当前未提供目标主机/认证信息；外部skill安装可能受网络限制。

## 154.12.21.24 部署进度（11:32 UTC）

- Decision: 先落地 PostgreSQL/Redis/Nginx 基础组件并验证，再继续安装 Node 与 sub2api。
- Current status: 已SSH登录并完成 `postgresql`、`redis-server`、`nginx` 安装与启动；PostgreSQL库/用户 `sub2api`/`zhanfan` 已创建；Redis已启用 `requirepass zf20131414` 且本机访问验证通过；80端口当前由Nginx监听。Node/npm 尚未安装，sub2api 尚未部署（缺仓库地址）。
- Next step: 安装 Node.js + npm（建议Node 20 LTS），拉取 sub2api 仓库，写入环境变量（DB/Redis），用 systemd 启动服务并由Nginx反代到80。
- Blockers: 未提供 sub2api 的 git 仓库地址（或现有代码目录）。

## 154.12.21.24 部署进度续更（13:22 UTC）

- Decision: 继续远程落地 sub2api，采用官方 `deploy/install.sh` 安装路径，并维持 Nginx 占用80端口（后续反代到 sub2api）。
- Current status: 已完成 Node.js/npm 安装（`node v18.19.1`, `npm 9.2.0`）；代码已克隆到 `/opt/sub2api`；官方安装脚本已启动并进入语言选择交互界面（等待输入）。PostgreSQL/Redis/Nginx 仍为 active。
- Next step: 在安装脚本中完成交互（语言、配置），启动并启用 `sub2api` systemd 服务；再配置 Nginx 反代 `:80 -> sub2api:8080`，做端到端健康检查。
- Blockers: 安装脚本是交互式流程，当前停在“语言选择”提示，需要继续输入才能完成安装。

## 154.12.21.24 部署完成反馈（13:27 UTC）

- Decision: 采用 sub2api 官方 `deploy/install.sh` 完成服务部署，Nginx 占用 80 并反向代理到 sub2api `127.0.0.1:8080`。
- Current status: `sub2api/nginx/postgresql/redis-server` 均为 active；监听状态为 `:80(nginx)`, `:8080(sub2api)`, `127.0.0.1:5432(PostgreSQL)`, `127.0.0.1:6379(Redis)`；`http://154.12.21.24` 已返回 sub2api 页面内容。
- Next step: 在浏览器打开 `http://154.12.21.24` 完成 sub2api 首次向导（填 PostgreSQL/Redis 与管理员账号）；若需要我可继续远程代填并做登录验证。
- Blockers: 首次向导需人工/远程继续提交业务配置（数据库连接细节、管理员账户）。
