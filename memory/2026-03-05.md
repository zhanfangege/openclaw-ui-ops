# 2026-03-05

- Task: 用户同意执行“固定EVOMAP_DEVICE_ID + 接入Hub + 控制上下文占用”三项优化。
- Decision: 修改 `bin/evolver_start.sh` 注入 `A2A_HUB_URL=https://evomap.ai` 与稳定 `EVOMAP_DEVICE_ID` 逻辑（持久化到 `evolver-runtime/evomap_device_id`）；并在 `AGENT_OPTIMIZATION.md` 新增上下文预算控制规则。
- Current status: evolver 已重启并运行，进程环境已确认包含 `EVOMAP_DEVICE_ID`、`A2A_HUB_URL`、`EVOLVE_STRATEGY=harden`。
- Next step: 持续观察 evolver 日志中的 Hub 交互与稳定性；上下文预算策略从当前会话开始执行。
- Blockers: 当前会话上下文历史已较高（81%），新策略对后续增量生效，无法回收既有上下文。

- Task: 用户要一张抖音9:16引流宣传图（免费部署 OpenClaw 主题）。
- Decision: 生成可直接发布与二次编辑的标准 1080x1920 SVG 海报，内容含卖点区、口播引导文案、头像位与二维码位。
- Current status: 已输出文件 `deliverables/douyin-openclaw-promo-9x16.svg`。
- Next step: 用户替换头像/二维码后可直接发；如需我可再给“极简版/冲击版”第二套。
- Blockers: 当前容器内 ImageMagick 缺中文字体，SVG->PNG 直接转换失败。

- Task: 调整抖音引流海报文案并取消头像
- Decision: 采用单二维码布局并强化公益协助定位
- Current status: new poster svg generated
- Next step: 按需再出A/B两版标题
- Blockers: 容器内字体不足，建议在本机设计工具导出PNG
- Task: 生成并迭代抖音引流海报（9:16），主题为“利用空闲时间免费部署 OpenClaw”。
- Decision: 重建 `deliverables/douyin-openclaw-promo-9x16.svg`，按用户要求改文案、移除头像区、保留单二维码主引导。
- Current status: 海报已完成并引用用户二维码文件 `deliverables/二维码.png`。
- Next step: 如需发布成 PNG/JPG，在本机设计工具或带中文字体环境导出，并可继续做 A/B 标题版本。
- Blockers: 当前容器 ImageMagick 缺中文字体，SVG 直转 PNG 失败。
- Git: 已提交 `769494e`（revise poster copy + remove avatar block）。
- Task: 用户要求“直接把二维码合成进去”。
- Decision: 将海报SVG中的二维码引用从相对路径改为内嵌 base64 data URI，避免外链丢失。
- Current status: `deliverables/douyin-openclaw-promo-9x16.svg` 已内嵌二维码，可单文件分发。
- Next step: 如需我可再导出一版纯图片（PNG/JPG）供直接发布。
- Blockers: 当前容器缺中文字体，直接栅格化中文海报存在字体兼容风险。
- Task: 用户要求“重新生成”抖音9:16海报。
- Decision: 全量重做海报文案与版式，移除看板承诺，保留“空闲时间免费部署”主叙事，并内嵌二维码为单文件。
- Current status: 新海报已生成 `deliverables/douyin-openclaw-promo-9x16.svg`（二维码已 data URI 内嵌）。
- Next step: 如需再做A/B，我可再出“极简标题版”和“强利益点版”。
- Blockers: 无。
- Task: 用户要求将目录下海报 SVG 转 JPG。
- Decision: ImageMagick 因字体问题失败后，改用临时 sharp 管道完成转换。
- Current status: 已生成 `deliverables/douyin-openclaw-promo-9x16.jpg`（1080x1920）。
- Next step: 可直接用于抖音发布；如需再压缩可导出 85 质量版。
- Blockers: 无。

- Task: 用户评估将 OpenClaw 从 Docker 迁移到 macOS 虚拟机，并请求对比与迁移建议。
- Decision: 采用“Docker 稳定核心 + macOS VM 高自由执行层”的混合架构建议，不建议一次性全量替换。
- Current status: 已完成 Docker vs macOS VM 对比、风险清单、PoC 并行验证建议；用户确认“愿意”进入下一步蓝图。
- Next step: 输出可执行的迁移蓝图（阶段计划、目录规范、版本锁定、快照回滚、代理 default-off/on-demand/auto-off 策略、验收标准）。
- Blockers: 无。

- Task: 用户要求“把 OpenClaw UI 运行起来”。
- Decision: 在容器内执行运行状态检查，并启动 `openclaw gateway` 前台进程（后台会话托管）以确保 UI 服务在线。
- Current status: `openclaw status` 显示 Gateway reachable；Dashboard 地址为 `http://172.18.0.5:18789/`；额外启动会话 `kind-pine` 正在运行。
- Next step: 从宿主机访问 `http://localhost:18789`（或 `http://<宿主机IP>:18789`）；若不通，补齐 Docker 端口映射 `18789:18789`。
- Blockers: 当前展示地址是容器内网 IP（172.18.x.x），宿主机需端口映射后才能访问。

- Task: 用户澄清要启动“我单独设计的看板”（openclaw-ui）。
- Decision: 在 `openclaw-ui` 目录以 `npm start` 启动独立 UI 服务，并保留后台会话托管。
- Current status: UI 已启动，监听 `0.0.0.0:4173`（后台会话 `good-ember`）；容器内 HTTP 检测返回 200。
- Next step: 用户从宿主机访问 `http://localhost:4173`；若不可达则补 Docker 端口映射 `4173:4173`。
- Blockers: 若容器未映射 4173 端口，宿主机无法访问该看板。

- Task: 用户要求恢复并固定通过 `192.168.100.196:18790` 访问独立看板。
- Decision: 终止原 `4173` 看板进程，改以 `PORT=18790` 重新启动 openclaw-ui。
- Current status: 新会话 `briny-crest` 运行中，监听 `0.0.0.0:18790`；本地健康检查 HTTP 200。
- Next step: 用户从宿主机验证 `http://192.168.100.196:18790`；如需持久化，再补 supervisor/compose 自启动。
- Blockers: 若宿主机未映射容器 18790 端口，则外网访问仍不可达。

- Task: 用户确认执行“看板自启动 + 崩溃自动拉起”。
- Decision: 新增 `bin/openclaw_ui_start.sh`、`bin/openclaw_ui_watchdog.sh`、`bin/openclaw_ui_watchdog_loop.sh`，以 60s 间隔巡检并自动恢复。
- Current status: 看板已由启动脚本拉起（PID 121963，端口 18790，健康检查 200）；watchdog loop 已后台运行（PID 121959）。
- Next step: 若宿主机重启后要自动恢复，需把 watchdog loop 接入宿主机启动器（systemd/compose/restart policy/crontab @reboot 其一）。
- Blockers: 当前容器环境无 systemd，进程级守护可用但“宿主机重启后自启”仍需宿主机层配置。
